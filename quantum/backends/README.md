# Quantum Backends (V.04 §3-§5)

Canonical implementation: `crates/quantum-runtime/src/backend.rs`.

Stable `QuantumProcessor` trait with Simulator / Emulator / Remote / Physical adapters. MajoranaBackend is capability-gated stub until documented hardware interface exists.