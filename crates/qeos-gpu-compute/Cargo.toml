[package]
name = "qeos-gpu-compute"
version = "0.4.4"
edition = "2021"
description = "QEOS P7.3 GPU compute: device API, memory safety, CPU reference, mock backend, failure recovery"
license = "MIT"

[lib]
name = "qeos_gpu_compute"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
