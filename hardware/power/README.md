# Hardware Power (V.04 §13)

Canonical implementation: `crates/hardware-abstraction/src/power.rs` (`PowerDevice`, `PowerInfo`, `PowerHealth` via `HardwareDevice` trait).

Access via `identify/initialize/health` only. No undocumented registers.