# Hardware SPI (V.04 §13)

Canonical implementation: `crates/hardware-abstraction/src/spi.rs` (`SpiDevice`, `SpiInfo` via `HardwareDevice` trait).

Access via `identify/initialize/health` only. No undocumented registers.