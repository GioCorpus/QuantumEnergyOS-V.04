# Hardware Telemetry (V.04 §13)

Canonical implementation: `crates/hardware-abstraction/src/telemetry.rs` (`TelemetryHardware`, `TelemetryInfo` via `HardwareDevice` trait).

Access via `identify/initialize/health` only. No undocumented registers.