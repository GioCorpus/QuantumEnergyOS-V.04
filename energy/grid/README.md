# Energy Grid (V.04 §17)

Canonical telemetry: `crates/energy-telemetry` (`EnergyService`, ring buffers, forecasting).

Grid role: monitor load, forecast, detect anomalies, optimize scheduling, visualize power flows, interface with supported inverters/meters. No free-energy claims; no antimatter capability.